{
  "name": "@tutods/lib",
  "private": true,
  "license": "MIT",
  "description": "ESLint configuration used in all my projects",
  "workspaces": [
    "packages/*"
  ],
  "repository": "https://github.com/tutods/lib",
  "scripts": {
    "format": "prettier --write ./packages",
    "pre:version:stable": "lerna version --conventional-commits --include-merged-tags --conventional-graduate --message \"chore(stable version): publish\"",
    "version:stable": "lerna publish from-git --no-private -no-verify-registry --yes",
    "pre:version:beta": "lerna version --conventional-commits --include-merged-tags --conventional-prerelease --preid beta --message \"chore(beta version): publish\"",
    "version:beta": "lerna publish from-git --no-private --canary --no-verify-registry"
  },
  "devDependencies": {
    "lerna": "^7.1.4",
    "prettier": "^3.0.0"
  }
}
